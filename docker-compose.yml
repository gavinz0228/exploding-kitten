version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: always
    environment:
      NODE_ENV: production
      LOG_DIR: /app/logs
    volumes:
      - .:/usr/src/app
      - ./logs:/app/logs
    command: npm start
